/**
 * Cette classe abstraite représente l'idée générale d'une commande
 * Nous permet de mettre toutes les commandes dans ArrayDeque (ou Array) de type <Commande>
 */
public abstract class Commande implements Expression{
}
